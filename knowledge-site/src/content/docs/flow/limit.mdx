---
title: Transaction Limits
slug: transaction-limits
sidebar:
 order: 4
---
import { txLimits } from '../../../components/data/transaction-limits';

<table class='w-full'>
    <tr> 
        <th className='whitespace-nowrap text-center'>Payment Method</th>
        <th className='whitespace-nowrap text-center '>Minimum Amount <br /> (MYR)</th>
        <th className='whitespace-nowrap text-center '>Maximum Amount <br /> (MYR)</th>
        <th className='whitespace-nowrap text-center '>Limit Type</th>
    </tr>
    {txLimits
    .filter((txLimit) => txLimit.channel !== "DuitNow")
    .map((txLimit) => (
        <tr className='text-center'>
            <td>{txLimit.channel}</td>
            <td>{txLimit.minimum}</td>
            <td>{txLimit.maximum}</td>
            <td>{txLimit.type}</td>
        </tr>
    ))}
</table>